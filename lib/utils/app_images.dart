import 'app_constants.dart';

class AppImages {
  ///svg type example
  //static final String comPhone = SVG_IMAGE_PATH + 'com_phone' + SVG_TYPE;
  static final String commonBackground = PNG_IMAGE_PATH + 'common_background' + PNG_TYPE;
  static final String logo = PNG_IMAGE_PATH + 'logo' + PNG_TYPE;
  static final String noTweets = PNG_IMAGE_PATH + 'no_tweets' + PNG_TYPE;
  static final String tweety = PNG_IMAGE_PATH + 'tweety' + PNG_TYPE;
}
